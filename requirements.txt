# Core ML / NLP
textblob==0.18.0       # Lightweight sentiment analysis (polarity scoring)

# LLM Integration
anthropic>=0.25.0      # Anthropic Claude API client

# API
fastapi==0.111.0       # Web framework for the /analyze endpoint
uvicorn==0.29.0        # ASGI server to run FastAPI
pydantic==2.7.1        # Data validation (used by FastAPI)

# Utilities
python-dotenv==1.0.1   # Load .env file for ANTHROPIC_API_KEY
